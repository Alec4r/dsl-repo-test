listView('Utilities') {
    jobs {
    }
    columns {
        buildButton()
        status()
        weather()
        name()
        lastSuccess()
        lastFailure()
        lastDuration()
        disableProject()
        lastBuildConsole()
    }
}
